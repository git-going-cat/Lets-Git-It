// TODO: import { MultiPage } from '@/features/multi/components/MultiPage'
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/multi')({
  component: () => <div />,
});
