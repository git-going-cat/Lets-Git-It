// TODO: import { ContributionPage } from '@/features/contribution/components/ContributionPage'
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/contribution')({
  component: () => <div />,
});
