/** CONFLICT 다음 명령어가 `git add <path>` 형태가 아닐 때(예외 케이스) 모달 헤더에 표시할 기본값. */
export const DEFAULT_CONFLICT_FILE = 'src/feature.js';

/** 싱글 모드 CONFLICT 미니게임 화살표 시퀀스 길이. 다른 모드는 각자 결정한다. */
export const CONFLICT_SEQUENCE_LENGTH = 5;
