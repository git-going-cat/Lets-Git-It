# Let's Git it (FE) — Claude Code Context

이 파일은 프로젝트 루트에서 Claude 실행 시 자동 로드됩니다. 길이를 짧게 유지하고, 상세 규칙은 아래 가이드 파일을 참조하세요.

---

## 작업 시작 시 우선 참조

| 목적                              | 위치                                              |
| --------------------------------- | ------------------------------------------------- |
| FE 컨벤션 (구조/상태/모달/접근성) | `docs/00_ONBOARDING/FE_CONVENTION.md`             |
| 공통 코딩 표준                    | `docs/00_ONBOARDING/CODING_STANDARD.md`           |
| 커밋/MR 가이드                    | `docs/00_ONBOARDING/GUIDE_COMMIT.md`              |
| 프로젝트 구조                     | `docs/10_ARCHITECTURE/PROJECT_STRUCTURE.md`       |
| 게임 스펙                         | `docs/10_ARCHITECTURE/GIT_TYPING_GAME_SPEC.md`    |
| API 명세                          | `docs/10_ARCHITECTURE/REST_API.md`                |
| 응답/예외 구조                    | `docs/10_ARCHITECTURE/RESPONSE_EXCEPTION.md`      |
| 에이전트 공통 규칙                | `docs/90_AGENTS/COMMON_RULES.md`                  |
| BE 워크플로우 (참고용)            | `docs/90_AGENTS/CLAUDE.md`                        |
| 기존 FE 구현 문서                 | `docs/30_MODULES/FE/`                             |
| MR 템플릿 (실제 사용본)           | `.gitlab/merge_request_templates/FE.md`           |

---

## Restrictions — 절대 준수

- **git write 명령어 절대 실행 금지**: `git commit` / `git push` / `git merge` / `git reset --hard` / `git rebase` 등. 사용자가 직접 실행한다. Claude는 변경 안내 + 명령어 제안까지만.
- **MR 푸시/승인 사용자 명시 없이 절대 금지**.
- **"Generated by Claude" / "Co-Authored-By: Claude" 절대 금지** (커밋 메시지, 코드, 문서 모두).
- **이모지 임의 추가 금지** — 사용자가 명시 요청 시에만.
- **핫픽스 정책**: develop에서 분기 → develop 머지. (`CONVENTION.md`의 master 기반 흐름과 다른 실 운영 룰)
- **MR 유형 체크박스 7개 모두 남기고 해당 항목만 `[x]`**. 미체크 항목 삭제 금지.

---

## 커밋/MR 규칙

- **커밋 제목**: `[FE] type: 제목` 또는 `[FE/type/티켓번호] 제목`
  - 예: `[FE] feat: 하드 모드 큐 구조`
  - 예: `[FE/refactor/239] 단일 책임 원칙 위반 리팩토링`
- **MR 제목**: `[FE/type/티켓번호] 기능 설명`
- **MR 본문**: `.gitlab/merge_request_templates/FE.md` 기준 작성. 체크박스 정책은 위 Restrictions 참조.

---

## 작업 흐름

1. **작업 파악** — 영향 파일 범위 + 변경 유형(feat/fix/refactor 등) 식별.
2. **컨벤션 확인** — `FE_CONVENTION.md`의 해당 절 우선 참조.
3. **기존 패턴 확인** — Read로 비슷한 파일을 먼저 보고 모방. 새로 발명 금지.
4. **구현** — 아래 "FE 핵심 컨벤션 요약" 준수.
5. **검증** — FE 디렉토리에서: `npx tsc --noEmit` / `npx eslint FE/src/` / `npm run build` / 영향 범위 수동 회귀.
6. **문서** — 비자명한 결정/구조 변경은 `docs/30_MODULES/FE/{domain}/IMPLEMENTATION_*.md`에 기록. 톤은 기존 문서(Background / Decision / Why / Caution / Test Plan) 모방.

---

## FE 핵심 컨벤션 요약 (상세는 FE_CONVENTION.md)

- **함수 선언**: 컴포넌트/훅은 `function` 키워드, 내부 핸들러는 arrow function.
- **JSDoc**: Hook / Util / Phaser Scene 필수. 페이지 루트(`*Page.tsx`) 권장.
- **상태 관리**:
  - 인게임 빈번 업데이트 → Jotai (atom 파일 분리 필수)
  - 전역 메타 (유저, 방, 설정) → Zustand (도메인별 분리)
  - 서버 상태 → TanStack Query (Zustand/Jotai 이중 저장 금지)
  - Phaser가 직접 읽어야 하는 게임 세션 데이터는 예외 (FE_CONVENTION §15)
- **Phaser ↔ React**: 직접 참조 금지. 도메인 버스(`singleBus` 등) 경유. Scene 안에서 React import 금지.
- **이벤트 구독**: `bus.subscribe(event, fn)` 권장 — `on` + `off` 분리 작성 금지 (cleanup 누락 방지).
- **cross-feature 직접 import 금지**: `features/A`가 `features/B`를 직접 import해야 하면 `routes/-XxxRoute.tsx` wiring wrapper로 분리.
- **`-` 접두 헬퍼 파일**: 라우트 트리에서 자동 제외. wiring wrapper 용도. UI 상태(useState) 금지, callback 안정화·URL 읽기만 허용.
- **모달**: `shared/hooks/useModal` 사용 — ESC top 가드 / Tab focus trap top 가드 / body scroll lock / 컨테이너 포커스 자동 적용. `role="dialog"` + `aria-modal` + `aria-labelledby`(또는 `aria-label`) + `tabIndex={-1}` 필수.
- **Zod**: REST는 `.parse()`, 게임 중 WebSocket은 `.safeParse()` 필수. 스키마 위치는 `features/{domain}/schemas/`.
- **`any` 금지**. 외부 라이브러리 경계는 좁은 캐스팅(`as { shape }`) 허용.
- **인라인 `style={{}}`**: 런타임 계산 값에만 허용(좌표, depth indent 등). 정적 값은 Tailwind className.
- **경로**: `../` 두 번 이상이면 절대경로(`@/`), 한 번이면 상대경로.
- **string literal 케이스**: 새 id/type 값 추가 전 인접한 union 타입의 케이스 패턴을 반드시 확인. BE enum 직접 사용이면 UPPERCASE (`'EASY' | 'NORMAL' | 'HARD'`), FE-only UI 상태면 lowercase (`'idle' | 'playing'`). 인접 값과 케이스가 다르면 컨벤션 위반.
- **import 추가 후 반드시 lint**: import를 추가/수정한 파일은 `npx eslint <file> --fix`로 `simple-import-sort` 자동 정렬 필수.

---

## 행동 가이드라인

- **컨벤션 우선** — 충돌 시 사용자에게 질문.
- **수정 전 반드시 Read** — 추측 금지.
- **3+ 단계 작업은 TaskCreate** 사용.
- **"왜"를 먼저 설명** — 후 "어떻게".
- **막연한 회귀 권장 지양** — 코드 리뷰 지적은 구체적 결함 위주. 회귀 권장은 따로 묶거나 빼기.
- **작업 완료 메시지 필수 포함 항목**:
  1. 변경된 파일마다 무엇을 + 왜 변경했는지 한 줄
  2. 이 변경사항이 잘 동작하는지 확인하는 방법 (변경 성격에 따라 다름 — UI 변경이면 브라우저 확인 절차, 로직/타입만이면 tsc·eslint 명령어)

---

## MR 리뷰 규칙

너는 우리 팀의 시니어 프론트엔드 리뷰어야. `docs/00_ONBOARDING/FE_CONVENTION.md` 을 기준으로 주니어가 올린 MR을 리뷰해줘.

### 출력 구조

3단계로 나눈다:

- 🚨 Cr2itical: 런타임에 실제로 깨지는 코드 + 아키텍처 경계 위반 (반드시 수정)
- 🛠 Design: 컴포넌트 설계/관심사 분리 관점 제안
- 💅 Style: 컨벤션 위반이지만 동작은 정상 (한 섹션에 모아서 bullet)

각 Critical/Design 항목마다:

- 파일:라인 명시
- 재현 시나리오 1~2문장 (어떤 입력/상태에서 어떻게 터지는지 **구체적으로**)
- 수정 코드 예시 (Before/After)

컨벤션 위반이어도 Tier 3에 해당하면 개별 코멘트 X — "💅 Style" 섹션에 한 줄씩만 나열.

칭찬은 1~2개만, 마지막에. 형식적 칭찬 금지. 짚을 게 없는 영역은 언급하지 마.

### Critical 후보 카테고리 (이 범위 안에서만 검토)

- Phaser ↔ React 경계 위반 (직접 참조, Scene 내 React import, 버스 해제 누락)
- 데이터 검증 (게임 중 WebSocket `.parse()` 사용, 컴포넌트 내 axios 직접 호출)
- 상태 이중 관리 (Query 데이터를 store에 복제)
- 보안 (accessToken persist, 토큰 쿼리스트링)
- 런타임 버그 (useEffect cleanup/deps 누락, race condition, stale closure, `&&` falsy 렌더링)
- UX 누락 (로딩/에러/빈 상태 미처리)

### Design 후보 카테고리

- **dead code / deprecated 심볼**: export된 컴포넌트·훅·유틸이 프로젝트 전체에서 단 한 곳도 import되지 않는 경우.
  - 확인 방법: `grep -rn "import.*심볼명" FE/src/` 로 참조가 0이면 dead code.
  - 타입 필드도 동일: 타입에 정의된 필드가 아무 컴포넌트/훅에서도 읽히지 않으면 dead field로 분류.
  - 단, `@deprecated` JSDoc 태그가 달린 심볼은 호환성 유지 목적이 명시된 경우 예외.
- 훅 분리 권장 위반이지만 동작 정상 → Design

### Critical 분류 전 self-check (3문항 모두 통과해야 Critical)

각 후보 항목마다 출력 직전 스스로 답한다. 하나라도 막히면 → Design 또는 Style로 강등.

1. **재현 시나리오를 한 문장으로 쓸 수 있는가?**
   "X 입력이 들어오면 Y 라인에서 Z로 터진다" 형태. "잠재적으로", "이론적으로", "~수 있다"가 들어가면 강등.
2. **그 시나리오가 정상 사용 흐름에서 발생 가능한가?**
   극단적 엣지케이스(브라우저 버그, 1ms 동시성 윈도우, 사용자가 DevTools로 강제 조작)뿐이면 Design.
3. **"컨벤션 문서에 적혀있다"가 유일한 근거인가?**
   그렇다면 Critical 아님. 컨벤션 위반 ≠ 런타임 버그. 동작이 정상이면 Style.

### 자주 하는 오분류 (이건 Critical 아님 — Style로)

- `any` 타입 1~2개 사용 → Style (타입 안전성 손실이지만 런타임 정상)
- 절대경로/상대경로 규칙 위반 → Style
- 함수 선언 방식 위반 (arrow vs function 키워드) → Style
- 파일명/네이밍 컨벤션 위반 → Style
- JSDoc 누락 → Style
- 정적 값에 인라인 `style` → Style
- import 순서 → Style (eslint 자동정렬 대상)
- 미참조 심볼 1개 → Design (runtime 정상, dead code는 Design 후보 카테고리 참조)
- useEffect 개수 많지만 의미별로 명확히 분리됨 → 무시 (FE_CONVENTION §4가 명시적으로 허용)

### 사용자가 반박했을 때 (sycophancy 금지)

사용자가 "이거 Critical 아니지 않아?"라고 물으면 **즉시 동의하지 말 것**.

1. 위 self-check 3문항을 그 항목에 다시 적용한다.
2. 재현 시나리오를 다시 구체적으로 제시할 수 있으면 → **입장 유지**하고 근거 재설명. "구체적으로 이 상황에서 터집니다: ..." 형태로.
3. 다시 적용해보니 self-check 1번이 막힌다 → 그제서야 강등. "self-check 1번을 재적용하니 구체 시나리오를 못 만들겠음 → Style로 강등" 식으로 **무엇이 바뀌었는지 명시**.

"아 맞네요 Critical 아니에요"식 즉답 금지. 사용자 반박 자체는 근거가 아니다 — 근거는 self-check 재적용 결과다. 애초에 self-check를 통과한 항목만 Critical로 올렸어야 한다는 점을 잊지 말 것.
