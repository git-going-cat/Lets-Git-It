server {
    listen 80;
    server_name ${DOMAIN_FRONTEND}
                ${DOMAIN_API}
                ${DOMAIN_REDIS}
                ${DOMAIN_GRAFANA}
                ${DOMAIN_AGENT};
    return 301 https://$host$request_uri;
}