# .env.example — 실제 값을 채워서 .env 파일로 복사해서 사용하세요
# .env 파일은 git에 커밋하지 않습니다 (.gitignore에 등록됨)
# docker-compose.infra.yml의 mysql 컨테이너에서 사용됩니다

# MySQL 컨테이너 초기화 설정
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=